{
  "private": true,
  "scripts": {
    "clean": "git clean -Xdf",
    "all": "npm run -ws --if-present fetch-proto && npm run -ws --if-present fetch-testdata && npm run -ws --if-present generate && npm run -ws --if-present format && npm run -ws --if-present build && npm run -ws --if-present update-exports && npm run -ws --if-present test && npm run -ws --if-present attw",
    "format": "prettier --write --ignore-unknown '.' '!packages'"
  },
  "workspaces": [
    "packages/cel-spec",
    "packages/cel-peggy",
    "packages/cel-antlr"
  ],
  "type": "module",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.17.0",
    "@bufbuild/buf": "^1.47.2",
    "@eslint/js": "^9.14.0",
    "@types/eslint__js": "^8.42.3",
    "@types/node": "^22.10.1",
    "eslint": "^9.14.0",
    "prettier": "^3.4.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.13.0"
  }
}
