{
  "private": true,
  "scripts": {
    "clean": "git clean -Xdf",
    "all": "npm run -ws --if-present generate && npm run -ws --if-present build && npm run -ws --if-present format && npm run -ws --if-present test && npm run -ws --if-present attw",
    "format": "prettier --write --ignore-unknown '.' '!packages'"
  },
  "workspaces": [
    "packages/cel-peggy",
    "packages/cel-antlr"
  ],
  "type": "module",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.16.4",
    "@bufbuild/buf": "^1.47.2",
    "@bufbuild/protoc-gen-es": "^1.10.0",
    "@eslint/js": "^9.14.0",
    "@types/eslint__js": "^8.42.3",
    "@types/node": "^20.17.6",
    "eslint": "^9.14.0",
    "prettier": "^3.3.3",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0"
  }
}
